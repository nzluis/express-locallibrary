<% layout('layout') -%>
<h1><% title %></h1>
<ul>
    <% book_list.forEach(book => { %>
        <li>
            <a href="<%= book.url %>"><%= book.title %></a>
            (<%= book.author.name %>) 
        </li>
    <% }); %>
    <% if (book_list == 0) { %>
        <li> There are no books </li>
    <% } %>
        
  
</ul>